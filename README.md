# Docker container for smashing

```
# Update volume paths on smashing.yml

# Launch with docker compose
docker-compose --file smashing.yml up
docker ps
docker log container_it

#Check on URL
http://serveurip:9080
```
